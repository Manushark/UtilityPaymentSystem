@model List<UtilityPaymentSystem.Controllers.Report>

<h1>Listado de Reportes</h1>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Fecha Generación</th>
            <th>Datos del Reporte</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var report in Model)
        {
            <tr>
                <td>@report.ReportId</td>
                <td>@report.GeneratedDate.ToString("dd/MM/yyyy")</td>
                <td>@report.ReportData</td>
                <td>
                    <a asp-action="Details" asp-route-id="@report.ReportId" class="btn btn-info">Detalles</a>
                    <a asp-action="Delete" asp-route-id="@report.ReportId" class="btn btn-danger">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="Create" class="btn btn-primary">Crear Nuevo Reporte</a>

